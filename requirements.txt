# This file lists the Python packages required to run the AI code scanner.
# Install them using the command: pip install -r requirements.txt

# Google Gemini AI SDK for code analysis
google-generativeai==0.8.3

# Cross-platform colored terminal output
colorama==0.4.6

# Template engine for dynamic prompt generation
jinja2==3.1.4

# YAML configuration file parsing
pyyaml==6.0.1

# Progress bar for file analysis
tqdm==4.66.1
